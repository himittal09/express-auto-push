{
  "name": "express-auto-push",
  "version": "0.1.0",
  "description": "HTTP/2 auto push library using express",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "build": "tsc",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "prepublish": "npm run build"
  },
  "keywords": [
    "node",
    "http2",
    "express",
    "static",
    "auto-push",
    "server-push"
  ],
  "author": "Himanshu Mittal <mittal01091997@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "h2-auto-push": "^0.4.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^12.0.7",
    "typescript": "^3.5.1"
  },
  "engines": {
    "node": ">8.0.0"
  },
  "files": [
    "LICENSE",
    "README.md",
    "CONTRIBUTING.md",
    "build/src"
  ]
}
